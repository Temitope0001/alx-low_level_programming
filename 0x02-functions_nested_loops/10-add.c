#include "main.h"
/**
*add - a function that adds two integeres and returns result
*@one: first number input
*@two: second number input
*Return: one + two
*/
int add(int one, int two)
{
	return (one + two);
}
